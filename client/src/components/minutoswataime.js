import React from "react";
import styled from "@emotion/styled";


const Container = styled.div`
    position:relative;
`;

function MinutosWT() {
    return (
        <Container>
        </Container>
    )
}

export default React.memo(MinutosWT);
